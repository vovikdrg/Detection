namespace Wangkanai.Detection.Regexs
{
    internal class AgentRegex
    {
        internal bool Match { get; private set; }
        protected readonly string agent;

        public AgentRegex(string agent)
        {
            this.agent = agent;
        }

        protected
    }
}
